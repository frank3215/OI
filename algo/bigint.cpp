#include <iostream>
#include <vector>
#include <string>
using std::cin;
struct bigint{
    vector<int> v;
}
std::istream& operator >> (bigint &b) {
    std::string s;
    std::cin >> s;
    for (int i = 
}
int main() {
    cin >> a >> b;
}
