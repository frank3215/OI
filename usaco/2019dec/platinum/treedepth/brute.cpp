#include <bits/stdc++.h>
using namespace std;

int f[maxn][maxn*maxn/2][maxn];

int dp(int n, int k, int i) {
	for (int l = 0, r; l < n; ++l) {
		r = n-1-l;
		for 
	}
}

int main() {
	scanf("%d%d%d", &n, &k, &m);
}
