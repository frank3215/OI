#include <bits/stdc++.h>
using namespace std;

const int maxn = 2010;

int n, f[maxn][maxn<<1];
char s[maxn];

int main() {
	scanf("%s", s+1);
	n = strlen(s+1);
	f[0][0] = 1;
	for (int i = 1; i <= n; ++i) {
		if (s != ')') {

		}
	}
