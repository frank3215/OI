#include <bits/stdc++.h>
using namespace std;

const int maxn = 2010;

int n, a, b;
int c[maxn], s[maxn];

namespace sub1{
    const int maxn = 2010;
    int f[maxn];
    void solve() {
	f[0] = 0;
	for (int i = 1; i <= n; ++i) {
	}
}


int main() {
    scanf("%d%d%d", &n, &a, &b);
    for (int i = 1; i <= n; ++i) {
        scanf("%d", c+i);
    }
    for  
    if (a == 1) sub1::solve();
    else sub2::solve();
}
