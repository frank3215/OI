#include <stack>
#include <cstdio>
int l,w,t;
bool err;
char s[100],c;
int main(){
	scanf("%d",&t);
	while(t--){
		scanf("%d O(n^%d",&l,&w);
		do{
			c=getchar();
		}while(c!='\n'&&c!='\r');
		err=0;
		while(l--){
			fgets(s,100,stdin);
			if(err) continue;
			c=s[0];
			if(c=='F'){
				scanf("%d%d%d",
		}
	}
}
