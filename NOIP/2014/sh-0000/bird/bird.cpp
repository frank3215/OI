#include <bit/stdc++.h>
int main() {
	scanf("%d%d%d", &n, &m, &k);
	for (int i = 0; i < n; ++i) {
		scanf("%d%d", &x+1, &y+1);
		f[0] = 1;
	}
	while (k--) {
		scanf("%d%d%d", &p, &l, &h);
		for (int i = 0; i <= l; ++i
}
