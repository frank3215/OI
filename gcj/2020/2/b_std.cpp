#include <bits/stdc++.h>
using namespace std;

int main() {
	scanf("%d", &t);
	for (int i = 1; i <= t; ++i) {
		scanf("%d%d", &n, &m);
		for (int i = 2; i <= n; ++i) {
			scanf("%d", x+i);
			if (x[i] < 0) s.pb(mp(-x[i], i));
			else t.pb(mp(x[i], i));
		}
		vector<int> v;
		v.pb(1); t[1] = 
		for (int i = 1; i <= n; ++i) {
		}
		for (int i = 1; i <= n; ++i) {
			scanf("%d%d", u+i, v+i);
		}
	}
